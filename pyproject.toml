[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cv"
version = "0.1.0"
description = "CV html rendering engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["jinja2>=3.1.5"]

[dependency-groups]
dev = ["ruff>=0.9.3"]
